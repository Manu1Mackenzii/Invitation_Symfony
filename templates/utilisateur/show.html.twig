{% extends 'base.html.twig' %}

{% block title %}Utilisateur{% endblock %}

{% block body %}
<div class="white-panel">
    <h1 >Liste des Utilisateur</h1>
    <a href="{{ path('app_utilisateur_index') }}">Retour sur la liste</a>

    <a href="{{ path('app_utilisateur_edit', {'id': utilisateur.id}) }}">Modifier</a>

    <div class="login-show">
        <table class = "login-reg-panel">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ utilisateur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ utilisateur.nom }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ utilisateur.mail }}</td>
            </tr>

        </tbody>
    </table>


    {{ include('utilisateur/_delete_form.html.twig', {'button_label': 'Supprimer'}) }}
{% endblock %}
