{% extends 'base.html.twig' %}

{% block title %}Invitation{% endblock %}

{% block body %}
<div class="white-panel">
    <h1 >Liste des Invitation</h1>
    <a href="{{ path('app_invitation_index') }}">Retour sur la liste</a>

    <a href="{{ path('app_invitation_edit', {'id': invitation.id}) }}">Modifier</a>


    <div class="login-show">
        <table class = "login-reg-panel">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ invitation.id }}</td>
            </tr>
            <tr>
                <th>Expediteur</th>
                <td>{{ invitation.expediteur }}</td>
            </tr>
            <tr>
                <th>Receveur</th>
                <td>{{ invitation.receveur }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ invitation.statut ? 'Envoyé' : 'En attente' }}</td>
            </tr>
        </tbody>
    </table>
    </div>
    {{ include('invitation/_delete_form.html.twig', {'button_label': 'Supprimer'}) }}

</div>
{% endblock %}
